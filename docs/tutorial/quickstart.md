# Quickstart

> _Stub. To be written before any v1.0 push. Tracked in `DEFERRALS.md`._

Goal: a 5-minute end-to-end first run. Install → `lacuna init` in a real project → bare `lacuna` to open the TUI → see one real gap → suppress it with a reason → exit.

Code blocks in this tutorial must be tested in CI. No rotting tutorials.
