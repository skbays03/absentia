# `lacuna.toml` Reference

> _Stub. To be filled in alongside config implementation._

Will document every option in:

- `[scan]` — include / exclude / languages
- `[mining]` — confidence threshold, min group size, predicate size
- `[selectors.*]` — directory, decorator, parent_class, name_pattern, cluster, manual
- `[output]` — default mode
- `[storage]` — `.lacuna/` location, retention
- `[[suppress]]` — project-wide suppressions

See `lacuna.toml.example` at the repo root for a working sample with comments.
